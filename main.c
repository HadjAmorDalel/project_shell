#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include"Structure.h"
#include"fonction.h"
int main()
{
    rep c[4] ;
     int numbr_reps, numbr_file;
     
     saisie(*nmbr_reps);
     saisie(*numbr_file)
      for (i=0;i<nmbr_reps;i++0)
       for(j=0;j<numbr_file;j++)
        {
 	     if verif(*c[i])=true
 	      {
		          printf("donner le nom de repertoire %d";i)
 	             scanf("%s";&c[i].name);
 	         
 	              scanf("%s";c[i][j].contenu);
			  
	       }	 
         }
         
         printf("c/>");
         switch (ch);
         {
         	case 
		 }
     return 0;
  }
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 


 void show(nom_rep)
 char ch , ordr_up , ordr_down;
 scanf(ch);
 	ordr_up1= concat("show "; char *nom_rep; "-o u");
	 ordr_up2= concat("show -o u"; char *nom_rep );
	 ordr_down1=concat("show -o d"; char *nom_rep );*
	 ordr_down2=concat("show "; char *nom_rep;"-o d" );
	 alphabtc1 =concat("show";char *nom_rep;"-o c")
	 alphabtc2 =concat("show -o c";char *nom_rep;)
	 
 switch (ch);
 {
    	case (*ordr_up1 || *ordr_up2): 
	     {
	     if (verif(nom_rep; reps;numb_reps)==false)
         affiche(help); 
         	 tri_croissant(char *tab[], int numb_rep)
         	 
         else if 
         affiche(nom_rep.contenu);
         }
  }
           case (alphabtc1 || alphabtc2) :
           	{
           		for(i=;i<(numb_rep)-1;i++)
           		for (j=0;j<(nmbr_fichier)-1;j++)
           		for(k=0;k<strlen(rep[i].contenu[j])-1;k++)
           		{if 
				   }
           		
			   }
            {
			  if (verif(nom_rep; reps;numb_reps)==false)
             affiche(help); 
             else 
             affiche(nom_rep.contenu);
            }
  
 printf("show ",nom_rep);
 
 if (verif(nom_rep; reps;numb_reps)==false)
 affiche(help);
 else if (verif(nom_rep; reps;numb_reps)==true
 affiche(nom_rep.contenu);

else
 	ch= concat("show ";nom_rep; "-o u");
	 sh=("show -o d";nom_rep );
	 scanf(cmd);
 	switch cmd
 	{
 		case ch:
 			
 	            case sh:
 			 	 tri_decroissant(char *tab[], int numb_rep)
	 }
 }
 
